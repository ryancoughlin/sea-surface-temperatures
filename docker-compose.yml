services:
  app:
    build: .
    volumes:
      - ./data:/data
      - ./output:/output
      - ./config:/config
    restart: "no"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./output:/output
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app